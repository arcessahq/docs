[project]
name = "arcessa-docs"
version = "0.1.0"
description = "Documentation site for Arcessa."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "zensical==0.0.46",
]
